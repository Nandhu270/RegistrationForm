<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Student</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        h2{
            margin-top: 25px;
            margin-bottom: 15px;
            text-align: center;
        }
        body{
            background-image: url('https://img.freepik.com/premium-photo/abstract-background-design-images-wallpaper-ai-generated_1007521-2316.jpg');
            background-size: cover;
        }
        .btn-outline-primary:hover{
        background: linear-gradient(45deg, #00DDFF, #FF00FF, #FFD700);
        border-radius: 10px;
        transition: background 0.3s ease, transform 0.3s ease; 
        transform: scale(1.05);
        animation: pulse 1s infinite; /* Add pulse animation */
        text-align: center;
    }
    form{
        margin-top: -45px;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
    }
    .btn {
        text-align: center;
        margin-top: 5px;
    }
    label{
        margin-top: 5px;
    }
    .form-control:hover,.form-select:hover{
        background: linear-gradient(45deg, #00DDFF, #FF00FF, #FFD700);
        border-radius: 5px;
        transition: background 0.3s ease, transform 0.3s ease; 
        transform: scale(1.05);
        animation: pulse 1s infinite; /* Add pulse animation */
    }
    </style>
</head>
<body>
    <div class="container">
        <h2>Edit Student Details</h2>
        <form method="post" action="/edit/<%= student._id %>">
            <div class="col-md-6">
                <label class="form-label" for="Firstname">First Name</label>
                <input type="text" class="form-control" id="Firstname" name="FirstName" placeholder="Enter First Name" value="<%= student.FirstName %>">
            </div>
            <div class="col-md-6">
                <label for="Lastname" class="form-label">Last Name</label>
                <input type="text" class="form-control" id="Lastname" name="LastName" value="<%= student.LastName %>">
            </div>
            <div class="col-md-6">
                <label for="Email" class="form-label">Email</label>
                <input type="email" class="form-control" id="Email" name="Email" value="<%= student.Email %>">
            </div>
            <div class="col-md-6">
                <label for="PhoneNumber" class="form-label">Phone Number</label>
                <input type="text" class="form-control" id="PhoneNumber" name="PhoneNumber" value="<%= student.PhoneNumber %>">
            </div>
            <div class="col-md-6">
                <label for="DateOfBirth" class="form-label">Date of Birth</label>
                <input type="date" class="form-control" id="DateOfBirth" name="DateOfBirth" value="<%= student.DateOfBirth %>">
            </div>
            <div class="col-md-6">
                <label for="Gender" class="form-label">Gender</label>
                <input type="radio" id="male" name="Gender" value="Male" <%= student.Gender === 'Male' ? 'checked' : '' %>> Male
                <input type="radio" id="female" name="Gender" value="Female" <%= student.Gender === 'Female' ? 'checked' : '' %>> Female
                <input type="radio" id="prefer-not" name="Gender" value="Prefer Not To Say" <%= student.Gender === 'Prefer Not To Say' ? 'checked' : '' %>> Prefer Not To Say
            </div>
            <div class="col-md-6">
                <label for="Address" class="form-label">Address</label>
                <textarea class="form-control" id="Address" name="Address"><%= student.Address %></textarea>
            </div>
            <div class="col-md-6">
                <label for="Country" class="form-label">Select Country</label>
                <select class="form-select" id="country" name="Country">
                    <option selected disabled>-- Country --</option>
                    <option value="India" <%= student.Country === 'India' ? 'selected' : '' %>>India</option>
                    <option value="America" <%= student.Country === 'America' ? 'selected' : '' %>>America</option>
                    <option value="England" <%= student.Country === 'England' ? 'selected' : '' %>>England</option>
                    <option value="Russia" <%= student.Country === 'Russia' ? 'selected' : '' %>>Russia</option>
                </select>                
            </div>
            <div class="col-md-6">
                <label for="PostalCode" class="form-label">Postal Code</label>
                <input type="text" class="form-control" id="PostalCode" name="PostalCode" value="<%= student.PostalCode %>">
            </div>
        <div class="btn">
            <button type="submit" class="btn btn-outline-primary">Update</button>
        </div>
        </form>
    </div>
</body>
</html>
